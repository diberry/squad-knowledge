# Agent Team History — Sample Data

Use these files with `squad-knowledge discover examples/.squad` to try the CLI.

Copy this directory into your own project as `.squad/` and replace with real agent logs.
